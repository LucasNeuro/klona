---
// Seção sobre a solução KaaS com Timeline Inteligente
---

<section class="py-12 sm:py-16 md:py-20 lg:py-24 bg-black text-white">
  <div class="max-w-6xl mx-auto px-3 sm:px-4 md:px-6 lg:px-8">
    <div class="text-center mb-12 sm:mb-16">
      <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black mb-4 sm:mb-6 leading-tight">
        <span class="text-yellow-400">Klonagem Inteligente</span>: Otimização de Precisão para Sua 
        <span class="text-red-400">Infraestrutura Tecnológica</span>
      </h2>
      <p class="text-lg sm:text-xl text-slate-300 max-w-4xl mx-auto leading-relaxed">
        Nossa IA investiga cada software como um detetive forense, removendo o desnecessário, 
        preservando o essencial e integrando o ideal.
      </p>
    </div>

        <!-- Timeline Inteligente -->
        <div class="relative pb-8">
      
      <!-- Fase 1: Auditoria IA -->
      <div class="relative flex flex-col sm:flex-row items-start sm:items-center mb-12 sm:mb-16 md:mb-20 timeline-card">
        <div class="flex-shrink-0 w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-br from-yellow-400 to-yellow-600 rounded-full flex items-center justify-center z-10 border-4 border-black shadow-2xl timeline-icon mb-4 sm:mb-0 sm:mr-4">
          <svg class="w-8 h-8 sm:w-10 sm:h-10 text-black animate-pulse" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
          </svg>
        </div>
        <div class="flex-1 w-full">
          <div class="bg-gradient-to-br from-gray-900/80 to-gray-800/80 backdrop-blur-lg rounded p-4 sm:p-6 md:p-8 border border-yellow-500/30 shadow-2xl hover:shadow-yellow-500/20 transition-all duration-500 hover:scale-105">
            <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4 mb-4 sm:mb-6">
              <div class="w-10 h-10 sm:w-12 sm:h-12 md:w-14 md:h-14 bg-yellow-500/20 rounded-full flex items-center justify-center animate-spin-slow flex-shrink-0">
                <svg class="w-6 h-6 sm:w-7 sm:h-7 md:w-8 md:h-8 text-yellow-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
              </div>
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-yellow-400 animate-fade-in leading-tight">Fase 1: Auditoria Inteligente com Especialistas Klona e IA</h3>
            </div>
            <p class="text-base sm:text-lg text-slate-300 mb-4 sm:mb-6 animate-slide-in leading-relaxed">
              Nossa equipe de especialistas humanos, aliada à IA avançada, atua como detetives forenses para investigar cada software e otimizar sua stack tecnológica.
            </p>
            <ul class="space-y-3 sm:space-y-4 text-slate-300">
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-yellow-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">1</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Mapeamento Tridimensional:</strong> Funcionalidades, custos reais e padrões de uso</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.1s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-yellow-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">2</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Identificação de Redundâncias:</strong> Features idênticas em sistemas diferentes</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.2s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-yellow-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">3</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Cálculo de ROI por Feature:</strong> Quanto cada funcionalidade realmente custa e vale</span>
              </li>
            </ul>
          </div>
        </div>
      </div>

      <!-- Fase 2: Klone Surgery -->
      <div class="relative flex flex-col sm:flex-row items-start sm:items-center mb-12 sm:mb-16 md:mb-20 timeline-card">
        <div class="flex-shrink-0 w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-br from-red-400 to-red-600 rounded-full flex items-center justify-center z-10 border-4 border-black shadow-2xl timeline-icon mb-4 sm:mb-0 sm:mr-4">
          <svg class="w-8 h-8 sm:w-10 sm:h-10 text-black animate-bounce" fill="currentColor" viewBox="0 0 24 24">
            <path d="M13 3c-4.97 0-9 4.03-9 9H1l3.89 3.89.07.14L9 12H6c0-3.87 3.13-7 7-7s7 3.13 7 7-3.13 7-7 7c-1.93 0-3.68-.79-4.94-2.06l-1.42 1.42C8.27 19.99 10.51 21 13 21c4.97 0 9-4.03 9-9s-4.03-9-9-9zm-1 5v5l4.28 2.54.72-1.21-3.5-2.08V8H12z"/>
          </svg>
        </div>
        <div class="flex-1 w-full">
          <div class="bg-gradient-to-br from-gray-900/80 to-gray-800/80 backdrop-blur-lg rounded p-4 sm:p-6 md:p-8 border border-red-500/30 shadow-2xl hover:shadow-red-500/20 transition-all duration-500 hover:scale-105">
            <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4 mb-4 sm:mb-6">
              <div class="w-10 h-10 sm:w-12 sm:h-12 md:w-14 md:h-14 bg-red-500/20 rounded-full flex items-center justify-center animate-pulse flex-shrink-0">
                <svg class="w-6 h-6 sm:w-7 sm:h-7 md:w-8 md:h-8 text-red-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
              </div>
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-red-400 animate-fade-in leading-tight">Fase 2: Klone Surgery - Otimização de Eficiência</h3>
            </div>
            <p class="text-base sm:text-lg text-slate-300 mb-4 sm:mb-6 animate-slide-in leading-relaxed">
              Removemos o desnecessário, preservamos o essencial, integramos o ideal
            </p>
            <ul class="space-y-3 sm:space-y-4 text-slate-300">
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-red-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">1</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Consolidação Inteligente:</strong> Uma plataforma unificada com as melhores features</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.1s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-red-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">2</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Migração Sem Trauma:</strong> Dados e processos transferidos com segurança</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.2s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-red-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">3</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Integração Nativa:</strong> Todos os sistemas conversando na mesma língua</span>
              </li>
            </ul>
          </div>
        </div>
      </div>

      <!-- Fase 3: Agentes IA -->
      <div class="relative flex flex-col sm:flex-row items-start sm:items-center mb-12 sm:mb-16 md:mb-20 timeline-card">
        <div class="flex-shrink-0 w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-br from-blue-400 to-blue-600 rounded-full flex items-center justify-center z-10 border-4 border-black shadow-2xl timeline-icon mb-4 sm:mb-0 sm:mr-4">
          <svg class="w-8 h-8 sm:w-10 sm:h-10 text-black animate-pulse" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.94-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/>
          </svg>
        </div>
        <div class="flex-1 w-full">
          <div class="bg-gradient-to-br from-gray-900/80 to-gray-800/80 backdrop-blur-lg rounded p-4 sm:p-6 md:p-8 border border-blue-500/30 shadow-2xl hover:shadow-blue-500/20 transition-all duration-500 hover:scale-105">
            <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4 mb-4 sm:mb-6">
              <div class="w-10 h-10 sm:w-12 sm:h-12 md:w-14 md:h-14 bg-blue-500/20 rounded-full flex items-center justify-center animate-pulse flex-shrink-0">
                <svg class="w-6 h-6 sm:w-7 sm:h-7 md:w-8 md:h-8 text-blue-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.94-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/>
                </svg>
              </div>
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-blue-400 animate-fade-in leading-tight">Fase 3: Implante de Agentes IA Especializados</h3>
            </div>
            <p class="text-base sm:text-lg text-slate-300 mb-4 sm:mb-6 animate-slide-in leading-relaxed">
              Cada departamento ganha um assistente IA personalizado
            </p>
            <ul class="space-y-3 sm:space-y-4 text-slate-300">
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-blue-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">1</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Agentes Verticais:</strong> Vendas, Suporte, Financeiro, RH com IA específica</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.1s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-blue-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">2</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Automação Inteligente:</strong> Processos que aprendem e evoluem</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.2s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-blue-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">3</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Insights Preditivos:</strong> Decisões baseadas em dados em tempo real</span>
              </li>
            </ul>
          </div>
        </div>
      </div>

      <!-- Fase 4: KaaS Contínuo -->
      <div class="relative flex flex-col sm:flex-row items-start sm:items-center mb-8 sm:mb-12 timeline-card">
        <div class="flex-shrink-0 w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-br from-green-400 to-green-600 rounded-full flex items-center justify-center z-10 border-4 border-black shadow-2xl timeline-icon mb-4 sm:mb-0 sm:mr-4">
          <svg class="w-8 h-8 sm:w-10 sm:h-10 text-black animate-pulse" fill="currentColor" viewBox="0 0 24 24">
            <path d="M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6z"/>
          </svg>
        </div>
        <div class="flex-1 w-full">
          <div class="bg-gradient-to-br from-gray-900/80 to-gray-800/80 backdrop-blur-lg rounded p-4 sm:p-6 md:p-8 border border-green-500/30 shadow-2xl hover:shadow-green-500/20 transition-all duration-500 hover:scale-105">
            <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4 mb-4 sm:mb-6">
              <div class="w-10 h-10 sm:w-12 sm:h-12 md:w-14 md:h-14 bg-green-500/20 rounded-full flex items-center justify-center animate-spin-slow flex-shrink-0">
                <svg class="w-6 h-6 sm:w-7 sm:h-7 md:w-8 md:h-8 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
              </div>
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-green-400 animate-fade-in leading-tight">Fase 4: KaaS Contínuo - Evolução Constante</h3>
            </div>
            <p class="text-base sm:text-lg text-slate-300 mb-4 sm:mb-6 animate-slide-in leading-relaxed">
              Não entregamos um produto - assumimos sua eficiência tecnológica
            </p>
            <ul class="space-y-3 sm:space-y-4 text-slate-300">
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">1</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Monitoramento 24/7:</strong> Otimização em tempo real</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.1s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">2</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Evolução Adaptativa:</strong> Sistema que melhora continuamente</span>
              </li>
              <li class="flex items-start gap-2 sm:gap-3 animate-fade-in-up" style="animation-delay: 0.2s">
                <div class="w-5 h-5 sm:w-6 sm:h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-black text-xs sm:text-sm font-bold">3</span>
                </div>
                <span class="text-sm sm:text-base"><strong>Treinamento Espelhado:</strong> Sua equipe evolui com a tecnologia</span>
              </li>
            </ul>
          </div>
        </div>
      </div>
        </div>

    <!-- CTA Final -->
    <div class="text-center mt-12 sm:mt-16">
       <div class="bg-gradient-to-r from-yellow-600/20 to-red-600/20 rounded p-4 sm:p-6 md:p-8 border border-yellow-500/30">
        <h3 class="text-xl sm:text-2xl md:text-3xl font-bold mb-3 sm:mb-4 leading-tight">
          <span class="text-white">Pronto para a Otimização de Eficiência?</span>
        </h3>
        <p class="text-base sm:text-lg text-slate-300 mb-4 sm:mb-6 leading-relaxed">
          Nossa <span class="text-yellow-400 font-bold">Análise KaaS Gratuita</span> identifica exatamente 
          quais softwares podem ser consolidados e quanto você pode economizar.
        </p>
        <a 
          href="#contato" 
          class="inline-flex items-center gap-2 bg-gradient-to-r from-yellow-500 to-red-500 text-white px-4 py-2 sm:px-6 sm:py-3 rounded-full font-bold text-sm sm:text-base md:text-lg transition-all duration-300 shadow-xl hover:shadow-2xl hover:-translate-y-1"
        >
          <span class="whitespace-nowrap">DESCOBRIR MEU DESPERDÍCIO</span>
        </a>
      </div>
    </div>
  </div>
</section>

 <style>
   
   /* Animação do Timeline */
   @keyframes timelineProgress {
     0% { transform: scaleY(0); }
     100% { transform: scaleY(1); }
   }
   
   .timeline-line {
     animation: timelineProgress 3s ease-out;
     transform-origin: top;
   }
  
  /* Animações dos cards */
  @keyframes fadeInUp {
    0% { 
      opacity: 0; 
      transform: translateY(50px); 
    }
    100% { 
      opacity: 1; 
      transform: translateY(0); 
    }
  }
  
  @keyframes fadeIn {
    0% { opacity: 0; }
    100% { opacity: 1; }
  }
  
  @keyframes slideIn {
    0% { 
      opacity: 0; 
      transform: translateX(-30px); 
    }
    100% { 
      opacity: 1; 
      transform: translateX(0); 
    }
  }
  
  @keyframes spinSlow {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
  
  @keyframes bounce {
    0%, 20%, 53%, 80%, 100% { transform: translate3d(0,0,0); }
    40%, 43% { transform: translate3d(0, -8px, 0); }
    70% { transform: translate3d(0, -4px, 0); }
    90% { transform: translate3d(0, -2px, 0); }
  }
  
  .timeline-card {
    animation: fadeInUp 0.8s ease-out;
  }
  
  .animate-fade-in {
    animation: fadeIn 1s ease-out;
  }
  
  .animate-slide-in {
    animation: slideIn 1s ease-out;
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.6s ease-out;
  }
  
  .animate-spin-slow {
    animation: spinSlow 3s linear infinite;
  }
  
  .animate-pulse {
    animation: pulse 2s infinite;
  }
  
  .animate-bounce {
    animation: bounce 2s infinite;
  }
  
  /* Hover effects dinâmicos */
  .timeline-card:hover {
    transform: translateY(-10px) scale(1.02);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .timeline-icon:hover {
    transform: scale(1.1) rotate(5deg);
    transition: all 0.3s ease;
  }
  
  /* Efeitos de brilho */
  .timeline-card:hover .bg-gradient-to-br {
    box-shadow: 0 0 30px rgba(255, 255, 255, 0.1);
  }
  
  /* Animação da linha do timeline */
  .timeline-line::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    animation: shimmer 3s infinite;
  }
  
  @keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
  }
</style>

 <script>
   // Timeline inteligente com scroll dinâmico
   document.addEventListener('DOMContentLoaded', function() {
     const timelineCards = document.querySelectorAll('.timeline-card');
     const timelineLine = document.querySelector('.timeline-line');
     const timelineIcons = document.querySelectorAll('.timeline-icon');
    
     // Intersection Observer para animações sequenciais melhoradas
     const observer = new IntersectionObserver((entries) => {
       entries.forEach((entry, index) => {
         if (entry.isIntersecting) {
           // Delay baseado na posição do card
           const delay = index * 600;
          
          setTimeout(() => {
            // 1. Primeiro: Animação do ícone
            const icon = entry.target.querySelector('.timeline-icon');
            if (icon) {
              icon.style.transform = 'scale(1.2)';
              icon.style.transition = 'transform 0.4s ease';
              setTimeout(() => {
                icon.style.transform = 'scale(1)';
              }, 200);
            }
            
            // 2. Segundo: Animação do card principal
            setTimeout(() => {
              entry.target.style.opacity = '1';
              entry.target.style.transform = 'translateY(0)';
            }, 100);
            
            // 3. Terceiro: Título aparece
            setTimeout(() => {
              const title = entry.target.querySelector('h3');
              if (title) {
                title.style.opacity = '1';
                title.style.transform = 'translateX(0)';
              }
            }, 200);
            
            // 4. Quarto: Descrição aparece
            setTimeout(() => {
              const description = entry.target.querySelector('p');
              if (description) {
                description.style.opacity = '1';
                description.style.transform = 'translateX(0)';
              }
            }, 300);
            
            // 5. Quinto: Lista de itens aparece sequencialmente
            const listItems = entry.target.querySelectorAll('li');
            listItems.forEach((item, itemIndex) => {
              setTimeout(() => {
                item.style.opacity = '1';
                item.style.transform = 'translateX(0)';
              }, 400 + (itemIndex * 150));
            });
            
          }, delay);
        }
      });
     }, {
       threshold: 0.3,
       rootMargin: '0px 0px -50px 0px'
     });
    
    // Configurar animações iniciais com sequência melhorada
    timelineCards.forEach((card, index) => {
      // Card principal
      card.style.opacity = '0';
      card.style.transform = 'translateY(30px)';
      card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
      
      // Ícone
      const icon = card.querySelector('.timeline-icon');
      if (icon) {
        icon.style.transform = 'scale(0.8)';
        icon.style.transition = 'transform 0.4s ease';
      }
      
      // Título
      const title = card.querySelector('h3');
      if (title) {
        title.style.opacity = '0';
        title.style.transform = 'translateX(-30px)';
        title.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
      }
      
      // Descrição
      const description = card.querySelector('p');
      if (description) {
        description.style.opacity = '0';
        description.style.transform = 'translateX(-30px)';
        description.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
      }
      
      // Itens da lista
      const listItems = card.querySelectorAll('li');
      listItems.forEach((item) => {
        item.style.opacity = '0';
        item.style.transform = 'translateX(-20px)';
        item.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
      });
      
      observer.observe(card);
    });
    
    // Efeito de hover dinâmico
    timelineCards.forEach((card) => {
      card.addEventListener('mouseenter', function() {
        this.style.transform = 'translateY(-10px) scale(1.02)';
        this.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.3)';
        
        // Animação do ícone no hover
        const icon = this.querySelector('.timeline-icon');
        if (icon) {
          icon.style.transform = 'scale(1.15) rotate(5deg)';
        }
      });
      
      card.addEventListener('mouseleave', function() {
        this.style.transform = 'translateY(0) scale(1)';
        this.style.boxShadow = '0 10px 20px rgba(0, 0, 0, 0.1)';
        
        const icon = this.querySelector('.timeline-icon');
        if (icon) {
          icon.style.transform = 'scale(1) rotate(0deg)';
        }
      });
    });
    
    // Animação da linha do timeline
    const timelineObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          timelineLine.style.animation = 'timelineProgress 3s ease-out';
        }
      });
    });
    
    if (timelineLine) {
      timelineObserver.observe(timelineLine);
    }
    
        // Animação da linha do timeline com progresso
        let timelineProgress = 0;
        const updateTimelineProgress = () => {
          const scrollTop = window.pageYOffset;
          const docHeight = document.documentElement.scrollHeight - window.innerHeight;
          const progress = Math.min(scrollTop / docHeight, 1);
          
          if (timelineLine) {
            timelineLine.style.transform = `scaleY(${Math.max(progress, 0.1)})`;
            timelineLine.style.transformOrigin = 'top';
          }
        };
    
     
     window.addEventListener('scroll', updateTimelineProgress);
     updateTimelineProgress();
   });
 </script>